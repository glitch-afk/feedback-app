type Props = {};

const Header = (props: Props) => {
  return (
    <div className="text-4xl lg:text-6xl leading-none font-extrabold tracking-tight  mb-8 sm:mb-10 text-transparent bg-clip-text bg-gradient-to-br from-purple-400 to-purple-800">
      Feedback UI
    </div>
  );
};

export default Header;
